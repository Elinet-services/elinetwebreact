import { useStepVerticalHeight } from './types';
export declare const useVerticalStepHeight: ({ stepRef, show, isValidated, children }: useStepVerticalHeight) => void;
export default useVerticalStepHeight;
