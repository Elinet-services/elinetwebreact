import { useStepHeightProps } from './types';
export declare const useStepHeight: ({ stepRef, setHeight, isActive, isValidated, isMobile, children, }: useStepHeightProps) => void;
export default useStepHeight;
